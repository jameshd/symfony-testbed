{% extends 'base.html.twig' %}

{% block title %}Post {{ id }} - {{post.title}} {% endblock %}

{% block body %}

<h3>{{post.title }}</h3>
<p>{{ post.text}}</p>

<a href="{{ path('blog_index') }}" > Home </a>
{% endblock %}
